# philosophy-explorer Lean verification library.
#
# Connectivity spike (Milestone 0): the only built module is `Sanity`,
# a trivial theorem proving the F# -> lake -> lean plumbing works.
# Deliberately Mathlib-free — see docs/formal-logic/formal-verification.md §7.
# The real deep embeddings (NDFormula / Deriv) arrive in Milestone 1.
name = "philosophy_explorer_lean"
defaultTargets = ["Sanity"]

[[lean_lib]]
name = "Sanity"
