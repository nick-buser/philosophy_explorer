-- Root module for the propositional natural-deduction deep embedding
-- (Milestone 1). Importing `NaturalDeduction` brings in both the syntax
-- (NDFormula) and the derivation judgment (Deriv).
import NaturalDeduction.Formula
import NaturalDeduction.Deriv
import NaturalDeduction.Examples
